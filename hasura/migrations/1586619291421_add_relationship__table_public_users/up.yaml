- args:
    name: builds
    table:
      name: users
      schema: public
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: builds
          schema: public
  type: create_array_relationship
