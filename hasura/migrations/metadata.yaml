version: 2
tables:
- table:
    schema: public
    name: builds
  event_triggers:
  - name: generate-build
    definition:
      enable_manual: false
      insert:
        columns: '*'
    retry_conf:
      num_retries: 0
      interval_sec: 10
      timeout_sec: 60
    webhook: http://localhost:8081